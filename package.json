{
  "name": "word-game",
  "version": "0.1.0",
  "private": true,
  "author": "Ed Shevtsov <call007.ed@gmail.com>",
  "homepage": "https://call007.github.io/word-clone/",
  "dependencies": {
    "new-component": "^4.0.0",
    "prettier": "^2.8.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "prebuild": "rm -rf build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "new-component": "new-component",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "prepare": "husky install"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "husky": "^8.0.3"
  }
}
